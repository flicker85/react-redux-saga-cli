#!/usr/bin/env node
const program = require('commander');

program
  .version(require('../package').version)
  .usage('<command> [options]')
  .command('new', 'create a new project')
  .command('add', 'add new route')
  .parse(process.argv);
